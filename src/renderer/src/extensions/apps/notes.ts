import type { ExtensionApp } from '../types'

export const notesExtension: ExtensionApp = {
  id: 'notes',
  name: 'Notes',
  description: 'Open floating notes window',
  action: 'open-notes-window'
}
